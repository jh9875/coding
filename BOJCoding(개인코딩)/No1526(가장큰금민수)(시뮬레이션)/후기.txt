4와 7로만 이루어진 수를 찾아야 한다.

그렇다고 1에서부터 N보다 작거나 같은수를 반복문으로 수를 증가시켜갈순 없었다.

그래서 완전탐색을 이용했다.

함수의 파라미터로 String형의 변수를 넣었다.

여기서 재귀호출로 함수를 부를때 현재 String변수에 +"4" 또는 "7"을 넣어서
호출하고 N보다 큰수가 있으면 num.substring(0, num.length()-1)를
숫자로 변환해서 반환했다.
