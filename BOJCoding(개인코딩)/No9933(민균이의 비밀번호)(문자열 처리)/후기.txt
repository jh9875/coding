문제를 보고 정답비율이 낮길래 어려운 문제라고 생각했는데
생각보다 쉽게 풀 수 있었다.
자바에서는 문자열을 뒤집을 수 있는 StringBuilder의 reverse가 있어서 그런거일수도
있다.

알고리즘은 이렇다.
String 배열에 문자열들을 다 넣어놓고,
for로 돌아가면서 reverse문자열을  만든다.
그런 후 안쪽에 for문 하나 더 만들어서 원본 문자열이랑 같은게 있는지 확인한다.

StringBuilder의 reverse가 없다고 가정하면,
아마 난 char배열을 만들어서 뒤집을것같다.