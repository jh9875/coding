깜빡한 점:
1. 배열을 파라미터로 보낼 때, 주소값을 보내기 때문에 보내진 값이 바뀌면 원본값도
바뀐다. 
2. 값의 범위확인
-> 지금도 두번째 시도에서 정답일 수 있었지만 값의 범위를 확인하지 못했다.
케이스를 
9
> > > > > > > > >
으로 해보면 (다행히 스택오버플로우는 일어나지 않는다.)
int의 범위를 넘어서는 것을 알 수 있다.

일단 내가 푼 방법은
하나하나 부호에 맞게 넣어보는 것이다.

처음하고 두번째 시도에서 틀린 원인은 파라미터로 값을 전달하면 주소값 전달이므로 
값이 변경되었을 시에 원본값도 변경되어 버린다.
그래서 파라미터에 수를 넘길때 String으로 해서 String+i로 해서 새로운 문자열로 넘겼다.
boolean[] visited를 넘길때도 새로 복사한 후에 넘기니까 됐다.

예제를 다르게 해보고 예외를 찾았어야 했다.