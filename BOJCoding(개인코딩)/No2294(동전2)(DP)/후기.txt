이번에도 재귀적 동적계획법으로 풀어봤다.

최소의 동전값을 구하는 것이기 때문에 동전1과 비슷하게 코드를 짜는데

만약 동전의 가치가 안맞을경우 maxValue인 10000을 초과하게끔 반환해서

-1을 출력하게 했다.

아니면 0을 반환하게 하고

함수를 호출할때

cache[index][sum] =Math.min(cache[index][sum],
			minCoins(i, sum +coin[i]) +1);

이런식으로 경우를 1씩 더해줬다.


재귀적 동적계획법이 이해하기는 쉬워도 반복적동적계획법보다 시간과 메모리가
더 많이 소요된다.

반복적 동적계획법도 연습할 필요가 있는것같다.

