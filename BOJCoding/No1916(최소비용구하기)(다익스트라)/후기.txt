처음에는 내가 아는 다익스트라 그래로 사용했다.

distance, visited 등을 이용했고, 예제는 정답대로 나왔지만
메모리 초과가 나왔다.

테스트를 돌려보니 중복된 연산을 여러번 하는것처럼 보였다.

그래서 다른 코드들을 보다가 생각해보니

약간의 알고리즘 차이였다.

distance배열을 갱신하는 부분에서 내가 처음에 했던 코드는

distance[next] =Math.min(distance[next], 현재노드+현재노드에서 다음으로 가는비용);

대충 이런식으로 했다.


이런식의 코드가 더 깔끔하다고 생각했기 때문이다.
근데 문제가 있었다.
정확히는 감이 안잡히지만 여기서 문제가 생긴게 분병하다.

수정한 코드는 다음과같다.

if(distance[next] >현재노드+현재노드에서 다음으로 가는비용)
{
	distance[next] =~~~;
	pq.offer();
}

이런식으로 하면 조건을 검사해서 중복된 점을 계산하는 것도 피하고
visited배열을 만들 필요도 없어진다.